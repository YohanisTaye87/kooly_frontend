import 'package:flutter/material.dart';

class MarketProfileTab extends StatelessWidget {
  const MarketProfileTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Profile Tab"),
    );
  }
}
